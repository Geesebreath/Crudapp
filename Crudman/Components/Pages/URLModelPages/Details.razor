@page "/urlmodels/details"
@using Microsoft.EntityFrameworkCore
@using Crudman.Models
@inject IDbContextFactory<UrlModelContext> DbFactory
@inject NavigationManager NavigationManager

<PageTitle>Details</PageTitle>

<h1>Details</h1>

<div>
    <h2>URLModel</h2>
    <hr />
    @if (urlmodel is null)
    {
        <p><em>Loading...</em></p>
    }
    else {
        <dl class="row">
            <dt class="col-sm-2">Order</dt>
            <dd class="col-sm-10">@urlmodel.Order</dd>
            <dt class="col-sm-2">URL</dt>
            <dd class="col-sm-10">@urlmodel.Url</dd>
            <dt class="col-sm-2">Status Code</dt>
            <dd class="col-sm-10">@urlmodel.StatusCode</dd>
            <dt class="col-sm-2">Connection Type</dt>
            <dd class="col-sm-10">@urlmodel.ConnectionType</dd>
            <dt class="col-sm-2">Timestamp</dt>
            <dd class="col-sm-10">@urlmodel.TimeCode</dd>
        </dl>
        <div>
            <a href="@($"/urlmodels/edit?id={urlmodel.Id}")">Edit</a> |
            <a href="@($"/urlmodels")">Back to List</a>
        </div>
    }
</div>

@code {
    private UrlModel? urlmodel;

    [SupplyParameterFromQuery]
    private int Id { get; set; }

    protected override async Task OnInitializedAsync()
    {
        using var context = DbFactory.CreateDbContext();
        urlmodel = await context.UrlModel.FirstOrDefaultAsync(m => m.Id == Id);

        if (urlmodel is null)
        {
            NavigationManager.NotFound();
        }
    }
}
